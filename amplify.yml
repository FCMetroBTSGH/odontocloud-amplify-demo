version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd odontocloud_amplify_demo   # 👈 add
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: odontocloud_amplify_demo/.next   # 👈 add folder prefix
    files:
      - '**/*'


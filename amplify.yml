version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install          # ← no “cd”
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next       # ← no sub-folder prefix
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*

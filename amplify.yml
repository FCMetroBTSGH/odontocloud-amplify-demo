version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd odontocloud_amplify_demo   #
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: odontocloud_amplify_demo/.next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
